# Bussystem API Configuration
# Provide your test credentials here. Example (DO NOT COMMIT real prod creds):
 # Frontend legacy (will be removed after migrating to backend fully)
 # VITE_BUSS_LOGIN=starok_md_test
 # VITE_BUSS_PASSWORD=your_password

 # Backend (preferred secure usage - not exposed to client bundle)
 BUSS_LOGIN=starok_md_test
 BUSS_PASSWORD=your_password
 BUSS_BASE_URL=https://test-api.bussystem.eu/server
VITE_BUSS_BASE_URL=https://test-api.bussystem.eu/server
VITE_BUSS_LOGIN=
VITE_BUSS_PASSWORD=

# Development Options
# Set to "true" to use mock data instead of real API calls
VITE_USE_MOCK_BUSSYSTEM=false

# Development Notes:
# - For development, the proxy configuration in vite.config.ts handles CORS
# - In production, you'll need to configure your server to handle CORS or use a backend proxy
# - Legacy variable names VITE_BUSSYSTEM_LOGIN / VITE_BUSSYSTEM_PASSWORD are still read for backward compatibility
# - Contact Bussystem to activate your dealer account for live data
# - When VITE_BUSS_LOGIN is empty, mock API is automatically used in development
